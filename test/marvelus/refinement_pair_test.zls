(* Unit tests for refinement pair types *)

(*
    TEST 1: stream pair declaration
*)
let node sim () =
	let rec ((x, y):{(v:int) * (w:int) | w >= 0}) = (1, 1) fby (-x, (if (x < 0) then -x else x)) in (x, y)
