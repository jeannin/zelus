# Installation

To use the pre-commit hook that checks the makefile, copy the `pre-commit-check` file to your `.git/hooks` directory 

`cp pre-commit-check .git/hooks/`

Turn the hook into an executable.

`chmod +x .git/hooks/pre-commit-check`